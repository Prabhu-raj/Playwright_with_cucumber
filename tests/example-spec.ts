import {test, expect } from '@playwright/test';

// test('has title', async ({ page }) => {
//   await page.goto('https://playwright.dev/');

//   // Expect a title "to contain" a substring.
//   await expect(page).toHaveTitle(/Playwright/);
// });

// test('get started link', async ({ page }) => {
//   await page.goto('https://www.johnsonhealthtech.com/us/eng/contact-us');

//   // Click the get started link.
//   await page.getByLabel('Region', { exact: true }).click();

//   await page.keyboard.type("India")

//   await page.keyboard.press('Enter')

//   // Expects page to have a heading with the name of Installation.
//   await expect(page.getByLabel('Region', { exact: true })).toHaveValue("India");
// });
